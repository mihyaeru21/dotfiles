# ==== User definition ====

snippet use
    use strict;
    use warnings;
    use utf8;
    use 5.20.0;
    

snippet subtest
    subtest '${1:test_name}' => sub {
        ${0:TARGET}
    };

snippet validator
    my $v; $v //= Data::Validator->new(
        foo  => 'Foo',
        hoge => +{ isa => 'HashRef', default => sub { +{} } },
    )->with(qw/Method/);
    my ($class, $args) = $v->validate(@_);
    my () = @$args{qw/  /};

